import React from 'react';

// The login system was removed from the demo. This component used to render
// Google / GitHub / Facebook SSO buttons. It's kept as an empty stub so the
// existing `UserLogin` popup (which is no longer reachable from the UI) and
// any lingering imports continue to compile without pulling in the abandoned
// social-login packages (react-google-login, react-github-login, etc.).
const LoginComponent = () => null;

export default LoginComponent;
